1)First, fire up the mining by "sudo n" command in the terminal.
2)Type xterm h1 h2 to open up two terminals
3)Also, apply all the network conditions for which all the commands are already given


Stop and Wait:
==> Given image file should be in the same directory as the source codes.
==> Here, first, run the receiver python file(python3 receiver.py).
==> And run the sender python file similarly.
==> The output image will show up in the same directory.


Go back N:
==> Do the same command as above.
==> Except for the sender.py file, it will ask the user to enter the window size.
==> And run the same
==> The output image will show up in the same directory.